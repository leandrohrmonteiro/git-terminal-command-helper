import React from 'react'

const StartLocalRepository = () => {
    return(
        <div>
            <h3>Terminal git command is: </h3>
            <h5>(inside the folder that will become the local repository)</h5>
            <p>git init</p>
        </div>
        
    )
}

export default StartLocalRepository